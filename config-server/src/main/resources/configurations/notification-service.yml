spring:
  data:
    mongodb:
      username: Haythem
      password: Haythem12345
      host: locahost
      port: 64001
      database: notification
      authentication-database: admin
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: paymentGroup,orderGroup
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringSerializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.trusted.packages: '*'
        spring.json.type.mapping: paymentConfirmation:haythem.payment.notification.PaymentNotificationRequest,orderConfirmation:haythem.ecommerce.kafka.OrderConfirmation

  mail:
    host: localhost
    port: 1025
    username: haythem
    password: haythem
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enabled: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000

server:
  port: 8040
